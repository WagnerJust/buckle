# AGENTS.md

**Before making any change or answering any question, read the spokes below that match your task. Rules in this repo and its spokes override your default behavior — when they conflict, follow the repo.**

## Routes

- For all changes, read `docs/agents.md`.
- For adding a new install target, see `docs/sops/add-install-target.md`.
- For cutting a release, see `docs/sops/release.md`.

## Pointer files

The following files are pointers — they all defer to this hub:

- `CLAUDE.md`
